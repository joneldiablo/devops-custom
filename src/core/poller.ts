/**
 * Placeholder for poller core
 */

// TODO: Implement poller
// - setInterval with POLL_INTERVAL
// - for loop (SEQUENTIAL) through repos
// - git fetch --all --prune each repo
// - detect changes with git rev-list
// - call worker for updates
// - log results
